helm:
  chart: .
  releaseName: "fleet-flask-hello-world"
  values: 
    ingress:
      enabled: true
      hosts:
        - host: "fleet-flask-hello-world.poc-applications.plgrnd.be"
          paths: 
            - path: "/"
